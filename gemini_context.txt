Mira el archivo completo en:
C:\Users\yochi\Desktop\freelance-web\frontend\src\app\dashboard\settings\page.tsx

El código tiene 400 líneas. Si Gemini lo necesita completo, usa esta ruta que está en tu compu.

Puntos clave que quizás están fallando:

1. **Sin padding en móvil** — los forms tienen `sm:p-6` pero sin un `p-4` por defecto, así que en móvil el contenido pega contra los bordes
2. **Section headers ocultos en móvil** — `hidden sm:flex` los oculta, bien
3. **Las secciones sin borde/fondo en móvil** — `sm:bg-white sm:rounded-2xl sm:border` significa que solo se aplican en desktop
4. **`space-y-1` entre secciones** — solo 4px de separación en móvil
5. **Los inputs todavía tienen `border border-gray-200 rounded-xl`** que sí se ve en móvil
6. **Avatar `w-20 h-20`** — 80px, bastante grande para móvil
7. **El email input tiene `bg-gray-50`** que se ve en móvil

El problema principal que me dijiste es que "todo se ve muy pequeño" — posiblemente falta un `p-4` por defecto para que los inputs no estén pegados a los bordes en móvil, y los textos en `text-sm` (14px) se vean pequeños.

Pregúntale a Gemini qué mejoras haría para que se vea bien en móvil sin romper el escritorio.
